# A shell interactive coding exercise

## Q0\. A script that creates an alias

## Q1\. A script that prints `hello user`\, where user is the current Linux user.

##Q2\. A script thad adds `\/action` to the `PATH`. `\/action` should be the last directory the shell looks into when looking for a program.

##Q3\. A script that counts the number of directories in the `PATH`\.

##Q4\. A script that lists environment variables.
